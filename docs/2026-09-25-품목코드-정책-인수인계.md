# 품목코드 정책 작업 인수인계

작성 시각: 2026-09-25 01:49 KST
브랜치: `feat/auth-workspace`
최종 커밋: `9fd48fcf737fd0fc6582237b1be51d5d7cdc251c`

## 완료된 구현

- 거래 이력이 없는 품목만 코드 변경 허용
- 품목 수정 UI에서 서버의 `canRename` 판정을 사용
- 거래 이력이 있으면 코드 필드 비활성화 및 안내 문구 표시
- 서버 `PUT/PATCH`에서 권한·workspace 확인 후 참조 이력 재검증
- 참조 범위:
  - 구매요청 라인
  - 구매 라인
  - 판매계획 라인
  - 견적 라인
  - `stockMovement`
  - `stockBalance`
- 참조 이력이 있으면 `409`와 참조 유형별 건수를 포함한 오류 body 반환
- UI reference 조회에 현재 `workspaceId`를 명시해 다중 workspace 판정 오류 방지
- PATCH static authorization contract와 runtime 시나리오 추가

## 검증 결과

- `npm test`: 110 pass / 0 fail / 0 skipped
- `npx tsc --noEmit`: 통과
- `git diff --check`: 통과
- CI `36026767399`: success
- GitHub `isolated-postgres`: success
- Edison 독립 정적 QA: PASS
- 격리 runtime 시나리오: CI에서 실행 및 성공
- Production 및 Neon `main`: 접근·변경하지 않음

## Preview 상태

Vercel Preview 자동 배포는 실패했다.

- 원인 추정: Preview 환경에 `DATABASE_URL`이 설정되지 않음
- `flow-desk-ashy` deployment: `3PGhMm9fTPaGvoVRqGgpuUW1mL8y`
- `flow-desk` deployment: `AcQXNNhwnvvq8kPruGPrPog5rYkP`
- Production DB 연결 문자열을 Preview에 넣어 해결하지 않음

로컬 개발 서버는 기동 및 종료를 확인했다.

- `/master-data`: HTTP 200, 페이지 컴파일 성공
- 비인증 `/api/items/ITEM001?workspaceId=test`: HTTP 401
- 로그인 세션과 실제 품목 데이터가 없어 이력 있음/없음 다이얼로그의 실제 클릭 검증은 미완료

## 내일 이어서 할 일

1. Preview 전용 Neon branch 사용 여부 결정
2. 사용하기로 하면 Production과 분리된 branch를 준비하고 사용자가 Vercel Preview 환경변수를 직접 입력
3. Preview 재배포 후 실제 화면 확인:
   - 이력 없는 품목: 코드 활성화·변경·저장·read-back
   - 이력 있는 품목: 코드 비활성화·참조 건수 안내
   - 직접 API 요청: `409` 및 원본 불변
4. Preview가 정상일 때만 Production 배포 여부를 별도 승인

## 금지 사항

- Production `DATABASE_URL`을 Preview에 연결하지 않는다.
- Neon `main`에 접근하거나 변경하지 않는다.
- Production 배포를 자동으로 진행하지 않는다.
- 연결 문자열·비밀번호·secret을 문서나 채팅에 기록하지 않는다.
