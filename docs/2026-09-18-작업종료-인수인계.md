# FlowDesk 작업 종료 인수인계

- 정리일: 2026-09-18
- 저장소: `gopubryu/flowDesk`
- 브랜치: `feat/auth-workspace`
- 최신 코드 기준 커밋: `e1a0aaa983e9ee8f854e7af7649bc4ce9d77011d`
- 이전 인수인계 문서 커밋: `df9a9f114ecd2959422d3914f8cb49cc47e153a8`
- 원격 SHA: 로컬과 일치

## 오늘 완료된 상태

### 인증·권한

- Better Auth 이메일·비밀번호 인증 적용
- workspace 생성·초대·초대 수락 흐름 적용
- `ADMIN`, `OPERATOR`, `VIEWER` 역할 정책 적용
- 주요 업무 API에 workspace·역할 권한 적용
- Production 인증 schema 및 기본 health check 검증 완료

### 격리 PostgreSQL runtime 검증

- GitHub Actions ephemeral PostgreSQL 16 service 사용
- baseline migration 후 실제 Prisma·route handler 경로 실행
- `items` runtime 시나리오 최종 승인:
  - workspace A/B 교차 GET·PUT·DELETE 거부
  - 거부 후 상대 workspace 데이터 무변경
  - VIEWER 쓰기 거부
  - OPERATOR 생성 허용·삭제 거부
  - ADMIN 삭제 허용
- test DB safety:
  - `127.0.0.1`, `localhost`, `::1`만 허용
  - `flowdesk_test` 또는 `_test` suffix DB명만 허용
  - 원격 host·Neon·비-test DB 거부
  - IPv6 hostname 정규화 검증
- fixture cleanup은 workspace 삭제, user 삭제, Prisma disconnect를 독립 실행
- 본문 오류와 cleanup 오류 동시 발생 시 primary 오류를 보존

## 검증 결과

- 로컬 unit test: `104 pass / 0 fail`
- TypeScript: 통과
- Build: 성공
- `git diff --check`: 통과
- 최종 runtime CI: [Run 35240838618](https://github.com/gopubryu/flowDesk/actions/runs/35240838618) — Success
- 최종 승인 기준 커밋: `e1a0aaa983e9ee8f854e7af7649bc4ce9d77011d`
- 공개 저장소 보안 점검: 별도 전체 Git 이력 secret scan을 후속 실행해야 함
- `.gitignore`에서 `.env*` 전체 무시, `.env.example`만 허용

## 운영·보안 주의사항

- Production DB와 Neon `main`은 runtime 테스트에 사용하지 않음
- Production 배포·DB schema 변경은 별도 승인 없이 수행하지 않음
- 비밀번호, API key, token, connection string을 채팅·문서·로그에 기록하지 않음
- 실제 키가 과거에 외부에 노출된 사실이 발견되면 Git 삭제만으로 충분하지 않으므로 즉시 폐기·재발급
- 현재 `items` runtime 승인은 전체 API 보안 승인으로 확대 해석하지 않음

## 남은 작업

1. ~~다른 API route의 실제 runtime workspace 격리 테스트~~ — 8개 모듈 완료: items·vendors·warehouses·departments·employees(code 기반), tasks·events·mails(id 기반). 커밋 `8c6fd81bae860e149490029eb9946eb0e2f45fcb`, CI run `35341326395`. 미적용 route: 구매·구매요청·판매계획·견적·재무·재고
2. Better Auth 실제 cookie/session/network 통합 테스트
3. workspace B 멤버 사용자 시나리오
4. 초대 이메일·만료·중복·동시성 E2E
5. 구매·판매·재고 전체 통합 테스트
6. 백업·복구·rollback 실제 검증
7. ~~VIEWER 화면 쓰기 버튼 숨김 또는 비활성화~~ — 완료 (커밋 `c89e6befba1c63b8eb740201210445d903b4689c`, CI run `35320056364`, 총괄 승인)
8. 전체 Production 보안 최종 점검
9. 내부 파일럿 및 일반 공개 전 최종 QA

## 사용자 조치 필요

- `gopubryu/flowDesk` 저장소에서 `isolated-postgres` job을 branch protection의 required status check로 등록해야 함
- 경로: Settings → Branches → `feat/auth-workspace` 규칙 → Require status checks to pass → `isolated-postgres`
- 에이전트에게는 저장소 admin 권한이 없어 수행 불가 (API 401)
- 미등록 상태에서도 CI는 실행되지만, 실패한 CI의 머지 차단은 강제되지 않음

## 다음 시작점

- 다음 우선 작업은 남은 API route(구매·판매·재고·재무·일정·메일 등)를 `lib/integration-fixture.ts`의 모듈 테이블에 추가해 동일 매트릭스로 확장
- `tasks`·`events`·`mails` ID 기반 route에 foreign id + own workspaceId 조합의 GET/PATCH/DELETE 404 및 상대 row snapshot 불변 검증 추가; 코드 커밋 `262aa8fe347805a62d70a0a6b61e4f07023f5af9`, CI run `35341939656`
- `employees` GET의 `where: { workspaceId }` 제거 mutant를 별도 branch에 push해 CI run `35341971483`가 실패하는 것을 확인; mutant branch 삭제 및 원복 완료
- runtime fixture setup 도중 workspace/user 생성이 중간 실패해도 이미 생성된 리소스를 추적·정리하도록 `withWorkspaceFixture`를 보강하고, setup 실패 및 body 실패 primary 보존 unit test를 추가함. 커밋 `983d395a9354fb648c1eef4f9993702a1d65160b`, CI run `35342983461` 성공
- finance API runtime 격리 시나리오 추가: 교차 workspace GET/PATCH/DELETE 차단, foreign id + own workspaceId PATCH/DELETE 404, VIEWER read-only, OPERATOR write-only, ADMIN delete 및 DB read-back. 커밋 `04d9da96c4964151f6a43b0c334dcc90fc3dee82`, CI run `35351301959` 성공
- purchase-requests runtime 격리 시나리오 추가: 목록/상세 GET, 교차 workspace PATCH/DELETE, foreign ID + own workspaceId 404, VIEWER/OPERATOR/ADMIN 권한 및 DB read-back. 커밋 `3fffbbcee511cbd522861d9cae2582d000906024`, CI run `35355142438` 성공
- sales-plans runtime 격리 시나리오 추가: 목록/상세 GET, 교차 workspace PATCH/DELETE, foreign ID + own workspaceId 404, VIEWER/OPERATOR/ADMIN 권한 및 DB read-back. 커밋 `090e948e9e62295f31a0f10882e27b6ac4071c6b`, CI run `35360648482` 성공
- quotations runtime 격리 시나리오 추가: 목록/상세 GET, 교차 workspace PATCH/DELETE, foreign ID + own workspaceId 404, VIEWER/OPERATOR/ADMIN 권한 및 DB read-back. 커밋 `8822f00ad6781e07e8a32e4bbf01c18959b71570`, CI run `35361430330` 성공
- inventory balances runtime read 격리 시나리오 추가: 교차 workspace 403, own-list 비누출, VIEWER/OPERATOR 조회 허용 및 상대 balance 불변. 커밋 `fe3df39ea6564ce9ea77b60ecd9d05c7b24a1d1c`, CI run `35362493311` 성공
- inventory adjustments runtime write 격리 시나리오 추가: 교차 workspace GET 403, VIEWER POST 403, OPERATOR 조정 201 및 balance read-back, 상대 balance 불변. 커밋 `66928204de6466a93e854663b0a1c5e2e4fcba4b`, CI run `35363600919` 성공
- inventory receipts runtime write 격리 시나리오 추가: 교차 workspace GET 403, VIEWER POST 403, OPERATOR 입고 201, balance·movement read-back. 커밋 `49bef6762657026e69ebf436fbbc9208cb0443de`, CI run `35364371533` 성공
- 각 route 확장 후 unit test, TypeScript, build, diff check, CI run을 모두 확인
- 이후 `@edison` 독립 read-only QA를 요청하고 결과를 `@jegallyang`에게 보고
- Production과 Neon `main`에는 접근하지 않음

## 2026-09-20 purchases bulk 승인 요청 범위

- `app/api/purchases/bulk/route.ts` runtime 시나리오 추가.
- 검증: 교차 workspace PATCH `403`, foreign ID + own workspaceId PATCH `409`, VIEWER PATCH `403`, OPERATOR PATCH `200` 및 status read-back, OPERATOR bulk DELETE `403`, ADMIN bulk DELETE `200` 및 삭제 read-back, 상대 workspace purchase status 불변.
- 코드 커밋 `73680599ac57f4c1e3479dc6aaa7097e40bad5bc`, CI run `35453713004` 성공, guard `pass >= 19`, `skipped == 0`.
- 로컬 `npm test` 110 pass / 0 fail, TypeScript·build·diff check 통과.
- `@edison` 독립 QA 및 `@jegallyang` 승인 전까지 미승인.
- 승인 완료: `@edison` QA 및 `@jegallyang` 최종 승인으로 18번째 runtime 범위에 포함됨.
- 관례 차이: bulk의 foreign ID PATCH는 다른 모듈의 `404`와 달리 `409`를 반환함. bulk에는 상세 조회 경로가 없고 트랜잭션 내 존재 검증 실패가 `409`로 매핑되며, 응답 메시지가 대상 존재를 드러내지 않으므로 보안 문제 없음. 코드 변경 불필요.
- 로컬 개발 도구 설정 파일 `Opencode.json`은 `.gitignore`에 추가해 커밋되지 않도록 처리함.

## 2026-09-20 purchase orders 승인 요청 범위

- `app/api/purchases/[id]/route.ts` DELETE catch에 `authError` 매핑을 추가해 ADMIN 외 역할의 `ForbiddenError`가 500이 아닌 403으로 반환되도록 수정.
- purchase orders runtime 시나리오 추가: 교차 workspace 목록 GET `403`, foreign ID + own workspaceId 상세 GET `404`, 교차 workspace DELETE `403`, VIEWER POST `403`, OPERATOR POST `201`, OPERATOR DELETE `403`, ADMIN DELETE `200` 및 DB read-back, 상대 workspace purchase line 불변.
- 실제 item master와 purchase line을 포함한 전용 fixture를 사용해 기존 generic fixture 한계를 해소함.
- 코드 커밋 `cf96c2b7875cea8a5b78b0b413cc63314f1278b7`, CI run `35452414032` 성공, guard `pass >= 18`, `skipped == 0`.
- 로컬 `npm test` 110 pass / 0 fail, TypeScript·build·diff check 통과.
- `@edison` 독립 QA 및 `@jegallyang` 승인 전까지 미승인으로 유지.

## 2026-09-20 shipment 승인 정리

### 확정된 root cause

- `app/api/inventory/shipments/route.ts`의 POST `catch`가 `authError(e)`를 호출하지 않아, VIEWER의 `ForbiddenError`가 `403`이 아닌 `500 {"error":"Forbidden"}`으로 반환되고 있었음.
- 단일 진단은 통과하고 full-suite만 실패한 이유도 이 누락된 매핑 때문임.
- 수정: POST `catch` 첫 줄에 `const auth = authError(e); if (auth) return auth;` 추가.

### 승인된 shipment 검증

- 교차 workspace GET `403`
- VIEWER POST `403`
- OPERATOR POST `201`
- 자체 balance `7 → 5` read-back
- 상대 workspace balance 불변
- shipment stock movement 생성 확인
- 코드 커밋 `1dde0aaeaadaab3dcfc085aed3b0bf50c7bd20ec`
- CI run `35450763273` 성공, guard `pass >= 17`, `skipped == 0`
- 로컬 `npm test` 110 pass / 0 fail, TypeScript·build·diff check 통과

### 현재 승인 범위

- 16개 모듈·엔드포인트: items, vendors, warehouses, departments, tasks, events, mails, employees, finance, purchase-requests, sales-plans, quotations, inventory balances GET, inventory adjustments, inventory receipts, inventory shipments

### 남은 미승인

- 구매 orders 전용 fixture
- sales/orders 등 기타 API
- 실제 Better Auth cookie/session/network 경로
- workspace B 실제 membership 로그인 흐름
- branch protection required status check 등록
- 전체 Git 이력 secret scan 및 Production 보안 최종 점검

## 2026-09-19 작업 종료 정리

### 완료

- `inventory shipments` runtime 확장을 세 단계로 조사함.
- 최소 fixture + OPERATOR shipment POST 진단 시나리오는 CI `35368210661`에서 성공하여 item master, stock balance fixture, shipment transaction 및 재고 차감 경로가 정상임을 확인함.
- 전체 권한 시나리오를 추가했을 때 CI가 실패하여, 교차 workspace GET 또는 VIEWER POST 권한 검증이 남은 실패 지점으로 좁혀짐.
- shipment 진단·미완성 테스트 변경은 모두 원복함. shipment는 승인 범위에 포함하지 않음.
- integration workflow의 `tee` 파이프가 테스트 실패를 숨기지 않도록 `set -o pipefail`을 유지함. CI `35367615738` 성공.

### 현재 승인 기준선

- 승인된 runtime 범위: 15개 모듈·엔드포인트, inventory receipts까지
- integration guard: `pass >= 16`, `skipped == 0`
- 최신 안정 기준선: 커밋 `88cf17c632f50f3a8494dd543a51c3c1bb1790fd` 계열 변경 포함
- 로컬: `npm test` 110 pass / 0 fail, TypeScript·diff check 통과
- working tree clean
- Production·Neon `main` 무변경

### 미승인·다음 작업

- inventory shipments: 교차 workspace GET 또는 VIEWER POST의 상세 실패 응답 확인 후 재검증
- 구매 orders: 실제 item·line·receipt/stock lifecycle fixture 필요
- sales/orders 및 inventory 전체 write API
- 실제 Better Auth cookie/network와 workspace B membership
- branch protection required status check 등록
- 전체 Git 이력 secret scan 및 Production 보안 최종 점검

### 주의

- shipment를 성공·승인으로 판정하지 않음.
- CI job 상세 로그는 repository admin 권한 부족으로 직접 다운로드할 수 없음.
- 다음 세션은 shipment fixture를 다시 녹색 처리하기보다, 권한별 GET/POST를 각각 분리한 최소 재현으로 시작할 것.

## 2026-09-23 보안 점검 갱신

- 전체 Git 이력 240개 커밋을 대상으로 알려진 credential 패턴을 값 미출력 방식으로 검사함.
- private key, OpenAI token, GitHub token, AWS access key 패턴은 탐지되지 않음.
- PostgreSQL URL 패턴 20건은 `.env.example`, CI test URL, 테스트 안전장치 입력값으로 확인됨. 실제 `.env`, `.env.local`, `.env.production`은 이력에 없음.
- 판정: 현재 알려진 패턴 기준 실제 자격증명 노출 없음. base64 분할값 등 패턴 밖의 값까지 보장하는 검사는 아님.
- 남은 운영 항목: branch protection required status check, Production 보안 최종 점검, 실제 Better Auth cookie/network E2E.

## 2026-09-23 인증 HTTP E2E 완료 갱신

- 승인된 runtime 범위: 39개 시나리오.
- 실제 HTTP cookie E2E 승인 범위: 인증 쿠키, membership/onboarding, invitation acceptance, 다중 workspace 선택.
- CI 기준: `35884316103` success, integration guard `pass >= 40`, `skipped == 0`.
- 최종 승인 커밋: `3c3fd6d1de92953c2edd31919e4dafdc3ba85b50`.
- 확인된 실제 HTTP 경로: signup/Set-Cookie, `/api/auth/me`, logout, wrong-password, workspace onboarding/read-back, invitation accept/mismatch/expired, 다중 membership selector.
- Production·Neon `main`은 접근·변경하지 않음.
- 남은 운영 항목: branch protection required status check, 세션 만료·갱신 경계, Production 보안·배포 판단.

## 2026-09-23 Production 사전 점검

- 고정 demo workspace 참조는 demo seed/reset 모듈에 한정됨.
- `POST /api/demo/reset`은 `NEXT_PUBLIC_ALLOW_DEMO_RESET`가 정확히 `"true"`가 아니면 `404`로 fail-closed 처리됨.
- CI workflow의 DB URL은 `127.0.0.1:54329/flowdesk_test` test-only 값이며 Production/Neon URL 또는 repository secret을 참조하지 않음.
- migration inventory는 baseline migration 1개이며 CI는 test DB에만 `prisma migrate deploy`를 실행함.
- 현재 저장소 HEAD: `694294176afc81baf4aa809ca1db823afbede018`, working tree clean.
- 이 점검은 Production 접근·변경·배포를 수행하지 않은 사전 점검이며, 실제 배포 승인을 대체하지 않음.

## 2026-09-23 작업 종료 인수인계

### 완료된 승인 범위

- 업무 API workspace/role/IDOR 격리: 39개 runtime 시나리오
- 실제 HTTP 인증 cookie E2E: signup, Set-Cookie, `/api/auth/me`, logout, wrong-password
- membership/onboarding E2E: workspace 생성, ADMIN read-back, duplicate `409`
- invitation E2E: valid accept `200`, workspace ID/OPERATOR read-back, email mismatch `403`, expired `404`
- 다중 workspace cookie E2E: membership 2건, A/B workspace ID·role exact, selector 없는 업무 API `400`, 명시 workspace 조회 `200`
- session expiry E2E: 만료된 DB session의 cookie 요청 `401`
- 전체 Git 이력 known-pattern secret scan 및 Production 사전 점검
- branch protection rule `feat/auth-workspace` 적용 확인

### 기준 상태

- 최신 커밋: `694294176afc81baf4aa809ca1db823afbede018`
- working tree: clean
- 원격 `feat/auth-workspace`: 최신 HEAD 일치
- 최근 Production 사전 점검 CI: [35889184578](https://github.com/gopubryu/flowDesk/actions/runs/35889184578) — success
- 인증/E2E 기준 CI: [35888080834](https://github.com/gopubryu/flowDesk/actions/runs/35888080834) — success
- 로컬 기준: `npm test` 110 pass / 0 fail, TypeScript·build·diff check 통과
- Production·Neon `main`: 최근 작업에서 접근·변경하지 않음

### 남은 작업

1. Production 배포 여부에 대한 사용자 승인
2. 운영 backup/read-back/rollback 절차 확인 후 별도 migration·배포 실행
3. branch protection 일반 PR 경로의 `isolated-postgres` required check 동작 확인
4. session refresh/updateAge 경계는 선택적 후속 범위

### 주의사항

- Production DB 변경·배포는 승인 전 수행하지 않음.
- branch protection은 활성화됐으나 admin direct-push bypass가 가능하므로 보호 규칙을 우회해 push하지 않음.
- 세션·초대 token, cookie 원문, password, DB URL, API key는 로그·문서에 기록하지 않음.
- 다음 세션은 이 문서의 기준 상태에서 재개하고 이미 승인된 runtime/E2E를 반복하지 않음.

## 일반 PR 경로 검증 결과

- 검증 PR: `#1` (검증 완료 후 close, merge하지 않음)
- PR branch에서 integration workflow를 실제 실행했고 `Integration tests / isolated-postgres (pull_request)`가 `Successful in 1m`으로 보고됨.
- 보호 브랜치 `feat/auth-workspace`에 required check가 실제로 적용되어 일반 PR merge 정보에 `Required`로 표시되고, 검증 PR은 승인 요구 때문에 merge 차단됨.
- 검증용 branch와 PR은 정리했으며 base branch에는 검증용 변경을 병합하지 않음.
- PR의 Vercel Preview check는 별도 환경 문제로 실패했으며 Production 배포 성공을 의미하지 않음.

## Production backup/read-back/rollback 사전 점검

- 사용자 승인 범위는 사전 점검만이며, Production DB·Neon `main`·Vercel Production에는 접근하거나 변경하지 않음.
- 저장소 migration은 `00000000000000_baseline` 1개이고, CI의 `prisma migrate deploy` 대상은 loopback ephemeral PostgreSQL test DB뿐임.
- workflow에는 Production/Neon connection string 또는 repository secret 참조가 없음.
- rollback은 baseline을 기존 운영 DB에 직접 재적용하는 방식이 아니며, 실제 운영 변경 전 별도 backup branch/read-back과 reviewed incremental migration이 필요함.
- 현재 호스트에는 Vercel CLI가 설치되어 있지 않아 Production project/environment read-back은 수행하지 않음.
- `.env.local` 존재 여부만 확인했으며 값은 출력하지 않음. `.env`와 `.env.production`은 없음.
- 결론: backup 생성·read-back·rollback 실행은 아직 수행하지 않았고, 배포 승인 전 운영 변경은 보류함.

## Production 승인 후 접근 점검 결과

- 사용자 승인에 따라 Production 진행 경로를 확인했으나, Neon Console의 현재 인증 조직에는 `scraper`와 `tiny-voice-28427921` 프로젝트만 표시되고 FlowDesk Production DB 프로젝트가 표시되지 않음.
- Vercel 대시보드에는 `flow-desk`와 `flow-desk-ashy` 프로젝트가 보이지만 두 프로젝트 모두 `No Production Deployment` 상태였음.
- 현재 호스트에는 Vercel CLI가 없고, 저장소에서 Production connection string을 확인하거나 추측하지 않음.
- 따라서 대상 Neon Production 프로젝트/branch와 대상 Vercel Production project가 식별되지 않아 backup·read-back·migration·배포를 안전하게 수행할 수 없음.
- 사용자 또는 관리자에게 정확한 Neon Production 프로젝트/branch와 Vercel Production project 권한이 준비되기 전까지 운영 변경을 중단함.

## 확인된 Neon backup branch

- 사용자 제공 URL의 대상은 Neon project `tiny-voice-28427921` 내 branch `flowdesk-prod-backup-20260915` (`br-twilight-wave-ayknells`)로 확인됨.
- branch parent는 `main`, 만료 없음, 생성 시각은 2026-09-16 00:19:08, storage 약 32.75 MB, compute 0이다.
- Backup & Restore 화면에서 main의 6시간 history window가 제공되고 snapshot은 없음.
- `Preview data`를 read-only로 실행했으나 Neon이 `Error connecting to database: signal is aborted without reason`을 반환해 historic data read-back은 완료되지 않음. Restore/Reset/Schema 변경은 실행하지 않음.
- branch에 Primary compute endpoint가 없어 Tables/SQL Editor도 직접 read-back할 수 없음. Production main의 compute를 중단하거나 backup branch에 compute를 생성하지 않음.
- 따라서 backup branch 존재는 확인했지만, backup 내용의 table/query read-back과 rollback 복원 검증은 아직 미완료다.

## 임시 compute read-back 결과

- 비용 최소화를 위해 backup branch에 1 CU 임시 compute `ep-silent-water-aywia5vz`를 생성하고 read-only SQL만 실행함.
- `pg_stat_user_tables` 기준 public schema 21개 테이블이 존재함: 업무·재고·workspace 테이블 포함.
- 확인된 21개 테이블의 estimated row count가 모두 `0`이었고, `User`/`Session` 등 Better Auth 테이블도 public schema 목록에 없었음.
- 이는 해당 branch가 현재 인증·업무 데이터가 포함된 운영 backup으로 검증되지 않거나, 2026-09-15 backup 시점이 auth schema rollout 이전일 가능성을 뜻함. Production backup으로 간주해 migration/rollback 기준으로 사용하지 않음.
- read-back 직후 임시 compute를 삭제했으며, backup branch와 Production main의 데이터·schema·compute는 변경하지 않음.

## 2026-09-24 current-data backup 검증

- `main`에서 `flowdesk-prod-backup-20260924` branch를 생성함. Branch ID `br-lively-waterfall-ay7g851u`, parent `main`, 자동 만료 2026-09-25 09:37 KST.
- branch 생성 시 `data and schema` 옵션을 사용해 현재 main의 데이터와 schema를 복사함. 기존 2026-09-15 branch는 사용하지 않음.
- 새 branch에서 실제 read-only SQL을 실행해 Better Auth와 업무 schema를 확인함: public table 27개, `User=2`, `Workspace=3`, `WorkspaceMember=2`, `Item=14`, `Session=1`.
- `Account`, `Invitation`, `Session`, `User`, `Verification`, `WorkspaceMember`를 포함해 현재 인증 schema가 존재함.
- branch 생성 시 자동으로 생긴 1 CU compute를 read-back 직후 삭제함. Production main compute와 데이터에는 변경 없음.
- 이 branch는 현재 backup read-back 기준을 충족하지만, 실제 restore 실행은 하지 않았음. Production migration 전 Vercel 대상 project와 reviewed migration 적용 절차를 별도로 확정해야 함.

## 2026-09-24 Vercel 대상 확인 및 배포 차단

- 사용자 제공 서비스 URL `https://flow-desk-ashy.vercel.app/dashboard`가 Vercel project `flow-desk-ashy` (`hub-323b`)에 연결됨을 확인함.
- 최신 preview deployment source는 `feat/auth-workspace`, commit `510b53f`였음.
- Preview build가 `Error: DATABASE_URL is not set` 및 `/api/auth/invitations/accept` page-data 수집 실패로 종료됨.
- 따라서 Production promotion/deploy는 실행하지 않음. Vercel Production 환경의 `DATABASE_URL`, `DATABASE_URL_UNPOOLED`, `BETTER_AUTH_SECRET`, `BETTER_AUTH_URL` 확인·주입 후 preview build를 다시 통과시켜야 함.
- connection string/secret 값은 출력·저장·채팅 공유하지 않음.

## 2026-09-24 Production 배포 결과

- 사용자 제공 URL `https://flow-desk-ashy.vercel.app/dashboard`에 연결된 Vercel project `flow-desk-ashy`에 Production redeploy를 실행함.
- 대상 source: `feat/auth-workspace`, commit `510b53f` (`docs: record current production backup verification`).
- Vercel deployment `8eQ7Fy3iTDMVjEYe2uQKQPbQFvAs`가 `Ready` 상태로 완료됨.
- Production URL `/dashboard` HTTP `200` 확인.
- unauthenticated `/api/auth/me` → `401 {"error":"Unauthorized"}` 확인.
- unauthenticated `/api/items` → `401 {"error":"Unauthorized"}` 확인.
- Production 환경변수 값은 출력하지 않았으며, `BETTER_AUTH_URL`만 비민감 값으로 추가함.
- DB migration SQL/Production DB data는 변경하지 않음. 현재 main schema가 이미 auth tables를 포함하므로 이번 배포는 code/config 배포만 수행함.

## Production migration/rollback read-only follow-up

- Neon main (`br-purple-pine-aytt3xos`)에서 read-only `information_schema.tables` 조회를 실행한 결과 `public._prisma_migrations`가 존재하지 않음.
- 따라서 운영 DB에는 Prisma migration history가 없을 가능성이 확인됨. `prisma migrate deploy` 또는 baseline DDL은 실행하지 않음.
- 후속 migration 정리 후보는 운영 schema와 baseline 일치 여부를 먼저 별도 read-only 비교한 뒤, 승인 후 `prisma migrate resolve --applied 00000000000000_baseline`을 검토하는 것임. 실행은 보류.
- rollback runbook(미실행): (1) 현재 Vercel deployment ID/commit 기록, (2) Production traffic 중지 또는 maintenance 승인, (3) Neon의 검증된 persistent backup branch를 원본으로 별도 복구 대상 branch 생성, (4) schema/table/row-count read-back, (5) 승인 후 대상 전환, (6) `/dashboard` 200 및 unauthenticated API 401 확인. 이 단계에서 main restore/reset 또는 데이터 삭제는 자동 실행하지 않음.
- branch protection은 PR required 및 `isolated-postgres` required check를 일반 PR에서 확인했으나, admin direct-push bypass는 존재함.
