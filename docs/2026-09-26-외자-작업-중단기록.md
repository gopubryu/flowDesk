# 외자 구매 기능 작업 중단 기록

## 기준 상태

- 저장소: `gopubryu/flowDesk`
- 브랜치: `feat/auth-workspace`
- 최신 커밋: `add7b4d`
- Production/Neon `main`: 변경 없음
- working tree: clean

## 완료 범위

- USD·JPY 외화 구매 등록·표시
- 통관일·과세환율 입력
- USD `외화금액 × 환율`
- JPY `외화금액 × 환율 ÷ 100`
- 원화 환산액 자동 계산·읽기 전용
- 서버의 `baseAmount` 재계산 및 클라이언트 값 무시
- Decimal 정밀도 보존 및 BigInt 고정 스케일 half-up 계산
- 회계반영 문서(`accountingReflect === true`) 외자 정보 수정 차단
- 통관 UI의 확정/미확정/회계반영 잠금 및 저장 오류 알림
- 격리 PostgreSQL runtime 검증

## 최종 CI/QA

- CI: https://github.com/gopubryu/flowDesk/actions/runs/36158856050
- 결과: success
- isolated runtime: 42 pass / 0 fail / 0 skipped
- migration, 전체 테스트, build, TypeScript, diff-check 통과
- 실행 guard 기준 41 통과
- 실제 runtime 검증:
  - `baseAmount` 단독 POST: `400`, DB 행 미생성
  - 기존 USD spoof PATCH: DB `baseAmount = 1300`
  - 불일치 foreign/rate/base PATCH: DB `baseAmount = 2600`
  - 내자 `baseAmount` 단독 PATCH: `400`
  - USD `1 × 1300 = 1300`
  - JPY `1000 × 950 ÷ 100 = 9500`
  - JPY `1 × 950 ÷ 100 = 10` (half-up)

## 참고

CI 과정에서 fixture의 필수 VAT 필드, 구매 line, itemCode 누락이 발견되어 테스트 fixture만 보완했다. 제품 코드의 검증 완화는 하지 않았다.

## 남은 범위

- 총괄의 최종 승인 절차
- 회계 화면의 수입 부가세 별도 표시
- Production migration 및 Production 배포는 별도 승인 전 금지
